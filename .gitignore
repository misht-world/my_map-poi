node_modules/

# Промежуточные данные пайплайна не коммитим — пересчитываются из API (CLAUDE.md).
# Исключения: сэмпл для тестов и итоговый geojson (его публикует GitHub Pages).
# data/* (не data/) — иначе git не заходит внутрь и не видит ре-включения ниже.
data/*
!data/sample/
!data/normalized/

# Сборка фронтенда
web/dist/
dist/
